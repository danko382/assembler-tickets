\textbf{¬ командах на јссемблере результат всегда пересылаетс€
по адресу первого операнда.}
\smallskip

\textbf{¬иды адресаций операндов:}
\begin{enumerate}
    \item{–егистрова€:
    
    \begin{lstlisting} 
        MOV AX,BX ;(BX)->AX
    \end{lstlisting};}
    \item{Ќепосредственна€:
    
    \begin{lstlisting} 
        MOV AX,25 ;25->AX
        CONST EQU 34h ;именована€ константа CONST
        MOV AX,CONST ;34h->AX
    \end{lstlisting};}
    \item{ѕр€ма€:
    
    ≈сли известен адрес пам€ти, начина€ с которого размещаетс€
    операнд, то в команде можно непосредственно указать этот
    адрес.
    \begin{lstlisting} 
        MOV AX,ES:0001
    \end{lstlisting}
    ES~--- регистр данных, 0001~--- смещение внутри сегмента

    ѕр€ма€ адресаци€ может быть записана с помощью
    символического имени, которое предварительно поставлено
    в соответствие некоторому адресу пам€ти.

    Ќапример, в сегменте ES содержитс€ директива Var\_p DW
    \begin{lstlisting}
        MOV AX,ES:Var_p ;((ES)+Var_p)->AX
        MOV AX,Var_p ;((DS)+Var_p)->AX
    \end{lstlisting};}
    \item{ освенно регистрова€:
    
     ак в регистровой адресации, только в регистре содержитс€
    не сам операнд, а адрес, в котором он содержитс€
    \begin{lstlisting}
        MOV AX,[SI]
    \end{lstlisting}
    ћогут использоватьс€ регистры:
    
    SI,DI,BX,BP,EAX,EBX,ECX,EDX,EBP,ESI,EDI
    
    Ќе могут использоватьс€:
    
    AX,CX,DX,SP,ESP;}
    \item{ѕо базе со смещением:
    
    \begin{lstlisting}
        MOV AX,[BX]+2 ;((DS)+(BX)+2)->AX.
        == MOV AX,[BX + 2]
        == MOV AX,2[BX]
        MOV AX,[BP+4] ;((SS)+(BP)+4)->AX.
    \end{lstlisting};}
    \item{ѕр€ма€ с индексированием:
    
    \begin{lstlisting}
        MOV AX,MAS[SI] ;((DS)+(SI)+MAS)->AX
    \end{lstlisting}
    
    MAS~--- адрес в области пам€ти.
    
    —имволическое им€ определ€ет начало массива, а переход
    от одного элемента к другому осуществл€етс€ с помощью
    содержимого индексного регистра;}
    \item{ѕо базе с индексированием:
    
    \begin{lstlisting}
        MOV AX,Arr[BX][DI] ;((DS)+(BX)+(DI)+Arr)->AX
    \end{lstlisting}
    
    Ёта адресаци€ используетс€ дл€ работы с двумерными
    массивами. —имволическое им€ определ€ет начало массива,
    с помощью базового регистра осуществл€етс€ переход
    от одной строки матрицы к другой, а с помощью индексного
    регистра~--- переход внутри строки.}
\end{enumerate}